<Project Sdk="Microsoft.NET.Sdk">

  <!-- No Marten, no Aspire.Npgsql, unlike event-sourcing/'s Notifications.Infrastructure: the
       replica and the notification send-state both live in memory here (see
       Persistence/InMemoryUserContactRepository.cs and InMemoryNotificationRepository.cs' own
       notes on why). The only durable dependency is the Kafka log itself. -->

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Abstractions" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Notifications.Application\Notifications.Application.csproj" />
    <ProjectReference Include="..\..\..\Shared\EventLog\EventLog.csproj" />
  </ItemGroup>

</Project>
