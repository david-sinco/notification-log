<Project Sdk="Microsoft.NET.Sdk">

  <!-- No Marten, no Aspire.Npgsql, unlike event-sourcing/'s Users.Infrastructure: this project's
       only durable dependency is the Kafka log itself (Shared/EventLog), because there is no
       private database in this architecture. -->

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Abstractions" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Users.Application\Users.Application.csproj" />
    <ProjectReference Include="..\..\..\Shared\EventLog\EventLog.csproj" />
  </ItemGroup>

</Project>
