@inject NavigationManager Nav

@code {
    protected override void OnInitialized() =>
        Nav.NavigateTo(
            $"authentication/login?returnUrl={Uri.EscapeDataString("/" + Nav.ToBaseRelativePath(Nav.Uri))}",
            forceLoad: true);
}
