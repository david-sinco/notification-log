---
title: "Procesos: reaccionar a los hechos"
description: "Un evento guardado dispara comandos futuros, cancela otros agregados y cruza la frontera hacia otro servicio."
sidebar:
  order: 9
  label: "9 · Procesos y reacciones"
---

import Callout from '../../../components/Callout.astro';
import { Code } from '@astrojs/starlight/components';
import { member, span, whole } from '../../../lib/code';
import subscriptionSource from '@code/Services/Rentals/NotificationLog.RentalService.Infrastructure/Messaging/Subscriptions/DomainEventsSubscription.cs?raw';
import routerSource from '@code/Services/Rentals/NotificationLog.RentalService.Application/Processes/ProcessRouter.cs?raw';
import visitProcessSource from '@code/Services/Rentals/NotificationLog.RentalService.Application/Processes/VisitProcess.cs?raw';
import lifecycleSource from '@code/Services/Rentals/NotificationLog.RentalService.Application/Processes/ListingLifecycleProcess.cs?raw';
import notificationsProcessSource from '@code/Services/Rentals/NotificationLog.RentalService.Application/Processes/ListingNotificationsProcess.cs?raw';
import schedulerInterface from '@code/Services/Rentals/NotificationLog.RentalService.Application/Abstractions/ICommandScheduler.cs?raw';
import schedulerImpl from '@code/Services/Rentals/NotificationLog.RentalService.Infrastructure/Scheduling/WolverineCommandScheduler.cs?raw';
import scheduledHandler from '@code/Services/Rentals/NotificationLog.RentalService.Infrastructure/Scheduling/ScheduledCommandsHandler.cs?raw';
import expireHandler from '@code/Services/Rentals/NotificationLog.RentalService.Application/Listings/Commands/ExpireListing/ExpireListingHandler.cs?raw';
import dispatcherSource from '@code/Services/Rentals/NotificationLog.RentalService.Infrastructure/Messaging/Publishers/WolverineNotificationDispatcher.cs?raw';

<Callout kind="concepto" title="Objetivos de este módulo">

- Ver cómo se consumen los eventos ya guardados, después de la transacción.
- Distinguir un *process manager* de una proyección: uno provoca cambios, la otra solo observa.
- Entender cómo se programa un comando para dentro de un mes, y por qué el handler debe tolerar que llegue tarde.
- Seguir un hecho de Rentals hasta convertirse en un mensaje para Notifications.

</Callout>

<div class="ddd-frase">
<strong>En una frase:</strong> una proyección lee los eventos para <em>saber</em>; un proceso los lee
para <em>hacer</em> — y lo que hace suele terminar en eventos nuevos.
</div>

## El consumidor de la historia

<Code
    code={member(subscriptionSource, 'public override async Task<IChangeListener> ProcessEventsAsync')}
    lang="csharp"
    title="Services/Rentals/.../Messaging/Subscriptions/DomainEventsSubscription.cs"
    mark={['await using var scope = _scopes.CreateAsyncScope();', 'await controller.RecordDeadLetterEventAsync(e, ex);']}
/>

Una *subscription* de Marten recibe páginas de eventos **ya confirmados** y hace tres cosas por cada uno:

<ol class="ddd-steps">
    <li>
        <strong>Filtrar.</strong> <code>if (e.Data is not IDomainEvent)</code> ignora cualquier cosa que no
        sea un evento de dominio.
    </li>
    <li>
        <strong>Abrir un <em>scope</em> propio.</strong> Cada evento se procesa con su propio contenedor de
        DI y, por tanto, su propia sesión de Marten y su propia transacción. Lo que haga el proceso
        <em>no</em> comparte transacción con el evento que lo disparó.
    </li>
    <li>
        <strong>Aislar los fallos.</strong> Una excepción no detiene la suscripción: se registra y el evento
        va a la <em>dead letter queue</em> de Marten. Un proceso roto no bloquea los siguientes.
    </li>
</ol>

<Callout kind="advertencia" title="La consecuencia que hay que asumir">

El proceso corre **después** del commit, en otra transacción. Eso significa que puede fallar habiendo tenido éxito el comando original, y que puede ejecutarse **más de una vez** si se reintenta.

Por eso todo lo que hace un proceso tiene que ser idempotente — y lo es, porque termina llamando a métodos de agregado que [no levantan evento si no hay nada que hacer](/event-sourcing/agregado).

Es el mismo razonamiento del [módulo 10 de DDD](/ddd/domain-events) sobre despachar eventos fuera de la transacción, pero aquí con una diferencia decisiva: **el evento ya está guardado**, así que no se puede perder. Lo peor que pasa es que la reacción se retrase o se repita.

</Callout>

## El enrutador

<Code
    code={member(routerSource, 'public async Task RouteAsync')}
    lang="csharp"
    title="Services/Rentals/.../Application/Processes/ProcessRouter.cs"
/>

Un `switch` que reparte cada hecho a quien tenga algo que hacer con él, más una llamada final a `_notifications.NotifyAsync` que se ejecuta **para todos** los eventos.

Fíjate en que vive en `Application`, no en `Infrastructure`: el enrutador es una decisión de negocio ("cuando se aprueba una publicación hay que programar su vencimiento"), no de plomería. Lo único que vive en `Infrastructure` es la suscripción que lo invoca.

## Tres procesos, tres cosas distintas

### 1 · Programar el futuro

<Code
    code={member(visitProcessSource, 'public async Task OnConfirmedAsync')}
    lang="csharp"
    title="Services/Rentals/.../Application/Processes/VisitProcess.cs · OnConfirmedAsync"
    mark={['_scheduler.ScheduleAsync']}
/>

Confirmar una visita programa **tres comandos futuros**: recordatorio 24 h antes, recordatorio 2 h antes, y autocompletado 72 h después de que termine la franja.

Y observa la guarda `if (slotStart - leadTime > now)`: si la visita es para dentro de una hora, el recordatorio de 24 h **no se programa**. Programar un comando para el pasado no tiene sentido.

<Code
    code={whole(schedulerInterface)}
    lang="csharp"
    title="Services/Rentals/.../Application/Abstractions/ICommandScheduler.cs"
/>

La abstracción es de una línea; la implementación, de tres:

<Code
    code={whole(schedulerImpl)}
    lang="csharp"
    title="Services/Rentals/.../Infrastructure/Scheduling/WolverineCommandScheduler.cs"
/>

Wolverine guarda el mensaje en su cola durable con hora de entrega. Cuando llega el momento, lo entrega al handler correspondiente:

<Code
    code={whole(scheduledHandler)}
    lang="csharp"
    title="Services/Rentals/.../Infrastructure/Scheduling/ScheduledCommandsHandler.cs"
/>

Cinco líneas que conectan cada comando programado con el handler de la capa de aplicación que ya existía.

<Callout kind="concepto" title="Por qué un comando programado tiene que ser tolerante">

<Code
    code={member(expireHandler, 'public async Task HandleAsync')}
    lang="csharp"
    title="Services/Rentals/.../Listings/Commands/ExpireListing/ExpireListingHandler.cs"
    mark={['is not { } listing', 'listing.Expire(_time.GetUtcNow());']}
/>

`ExpireListingCommand` se programó hace un mes. En ese mes pueden haber pasado muchas cosas: la publicación se cerró, se retiró, se renovó, o el flujo ni siquiera existe ya.

El handler no comprueba ninguna de esas situaciones. **Se las delega al agregado**, y `Listing.Expire` simplemente vuelve sin hacer nada si la publicación ya no es visible o si su fecha de vencimiento cambió:

```csharp
// Listing.Expire — la guarda que hace inofensivo un comando que llega tarde
if (!IsVisible || ExpiresAt is not { } expiresAt || expiresAt > now)
    return;
```

Ese es el patrón entero de los comandos programados: **el agregado es quien decide, en el momento de la entrega, si el comando sigue teniendo sentido**. El proceso no tiene que cancelar nada cuando el mundo cambia.

</Callout>

### 2 · Reaccionar cambiando otros agregados

<Code
    code={member(lifecycleSource, 'public async Task OnNoLongerAvailableAsync')}
    lang="csharp"
    title="Services/Rentals/.../Application/Processes/ListingLifecycleProcess.cs"
    mark={['visit.Cancel(VisitParty.Host, UnavailableReason, now);', 'await _uow.SaveChangesAsync(ct);']}
/>

Cuando una publicación se cierra o se retira, sus visitas futuras se cancelan. Aquí está el patrón que la [Regla 4 de Vernon](/ddd/agregados) prescribe, hecho realidad:

- La transacción que cerró el `Listing` tocó **un solo agregado**.
- Este proceso, **después y por separado**, modifica los `Visit` afectados.
- Cada cancelación es un evento nuevo en su propio flujo, que a su vez disparará sus propias reacciones.

Eso es **consistencia eventual entre agregados**, y es exactamente lo que el módulo 7 de DDD describía sin poder enseñar, porque Notifications no tiene ningún caso.

<Callout kind="honesto" title="Un hueco real, anotado en todo.md">

`ProcessRouter` reacciona a `ListingClosed` y `ListingWithdrawn`, pero **no a `ListingExpired`**. Cuando una publicación vence, sus visitas futuras se quedan ahí.

Está anotado: *"Al vencer un `Listing` (`ListingExpired`), cancelar sus visitas futuras y avisar a los visitantes"*. Es un `case` que falta en el `switch` — y es la primera [tarea](/event-sourcing/tareas) de esta guía, precisamente porque el arreglo es de una línea y la lección de por qué se escapó es valiosa.

</Callout>

### 3 · Cruzar la frontera

<Code
    code={member(notificationsProcessSource, 'public Task NotifyAsync')}
    lang="csharp"
    title="Services/Rentals/.../Application/Processes/ListingNotificationsProcess.cs"
/>

Este es el punto donde un evento de dominio **se convierte en un evento de integración**.

`ListingApproved` es privado de Rentals: un `record` de C#, sin versión de esquema, que ningún otro servicio conoce. Pero el negocio quiere que el propietario reciba un correo, y eso vive en otro bounded context.

La traducción es explícita, y lo que sale por el otro lado es un contrato público:

<Code
    code={member(dispatcherSource, 'public async Task DispatchAsync')}
    lang="csharp"
    title="Services/Rentals/.../Messaging/Publishers/WolverineNotificationDispatcher.cs"
    mark={['new NotificationDispatchRequested']}
/>

`NotificationDispatchRequested` es el protobuf que [Notifications consume](/ddd/context-map) — con su `SchemaVersion`, su `EventKey` en formato `dominio.accion` (`publicacion.aprobada`, `visita.confirmada`) y su diccionario de datos libres.

<Callout kind="concepto" title="Esta es la respuesta a la pregunta del módulo 10 de DDD">

Aquel módulo decía que un evento de dominio **puede** convertirse en uno de integración, pero que la traducción debe ser explícita, porque es el sitio donde decides qué parte de tu modelo haces pública.

Aquí está esa traducción, en un archivo con nombre propio. Lo que Rentals publica hacia fuera es una lista cerrada de once claves (`NotificationKeys`), no sus quince eventos de dominio. **Cambiar `ListingApproved` no rompe a nadie; cambiar `publicacion.aprobada`, sí.**

</Callout>

## El ciclo completo

<div class="ddd-diagram">
    <svg viewBox="0 0 880 330" role="img" aria-labelledby="procTitle">
        <title id="procTitle">Del comando al evento, del evento a más comandos</title>
        <defs>
            <marker id="pcArrow" markerWidth="8" markerHeight="8" refX="6" refY="4" orient="auto">
                <path d="M0,0 L8,4 L0,8 Z" fill="#94a3b8" />
            </marker>
        </defs>

        <rect x="20" y="30" width="170" height="60" rx="10" fill="#eef2ff" stroke="#c7d2fe" />
        <text x="105" y="54" text-anchor="middle" font-size="10" font-weight="700" fill="#4338ca">Comando</text>
        <text x="105" y="72" text-anchor="middle" font-size="8.5" fill="#334155">ReviewListing (aprobar)</text>

        <line x1="192" y1="60" x2="228" y2="60" stroke="#94a3b8" stroke-width="1.5" marker-end="url(#pcArrow)" />

        <rect x="230" y="30" width="180" height="60" rx="10" fill="#fff7ed" stroke="#fed7aa" />
        <text x="320" y="54" text-anchor="middle" font-size="10" font-weight="700" fill="#9a3412">Evento guardado</text>
        <text x="320" y="72" text-anchor="middle" font-size="8.5" font-family="monospace" fill="#334155">ListingApproved · v4</text>

        <line x1="320" y1="92" x2="320" y2="122" stroke="#94a3b8" stroke-width="1.5" marker-end="url(#pcArrow)" />
        <text x="392" y="112" text-anchor="middle" font-size="7.5" fill="#64748b">tras el commit</text>

        <rect x="200" y="124" width="240" height="52" rx="10" fill="#f8fafc" stroke="#e5e9f2" />
        <text x="320" y="146" text-anchor="middle" font-size="10" font-weight="700" fill="#475569">DomainEventsSubscription</text>
        <text x="320" y="164" text-anchor="middle" font-size="8.5" fill="#334155">scope propio · errores aislados</text>

        <line x1="320" y1="178" x2="320" y2="206" stroke="#94a3b8" stroke-width="1.5" marker-end="url(#pcArrow)" />

        <rect x="200" y="208" width="240" height="44" rx="10" fill="#f0fdf4" stroke="#bbf7d0" />
        <text x="320" y="235" text-anchor="middle" font-size="10" font-weight="700" fill="#14532d">ProcessRouter</text>

        <line x1="442" y1="150" x2="500" y2="90" stroke="#94a3b8" stroke-width="1.3" marker-end="url(#pcArrow)" />
        <line x1="442" y1="228" x2="500" y2="180" stroke="#94a3b8" stroke-width="1.3" marker-end="url(#pcArrow)" />
        <line x1="442" y1="240" x2="500" y2="272" stroke="#94a3b8" stroke-width="1.3" marker-end="url(#pcArrow)" />

        <rect x="505" y="40" width="345" height="72" rx="10" fill="#fdf2f8" stroke="#fbcfe8" />
        <text x="677" y="62" text-anchor="middle" font-size="9.5" font-weight="700" fill="#9d174d">Programar comandos futuros</text>
        <text x="677" y="80" text-anchor="middle" font-size="8.5" fill="#334155">WarnListingExpiry (−7 días) · ExpireListing (+1 mes)</text>
        <text x="677" y="98" text-anchor="middle" font-size="8" fill="#64748b">cola durable de Wolverine</text>

        <rect x="505" y="144" width="345" height="72" rx="10" fill="#fdf2f8" stroke="#fbcfe8" />
        <text x="677" y="166" text-anchor="middle" font-size="9.5" font-weight="700" fill="#9d174d">Cambiar otros agregados</text>
        <text x="677" y="184" text-anchor="middle" font-size="8.5" fill="#334155">cancelar visitas futuras de esa publicación</text>
        <text x="677" y="202" text-anchor="middle" font-size="8" fill="#64748b">otra transacción · eventos nuevos</text>

        <rect x="505" y="248" width="345" height="62" rx="10" fill="#fdf2f8" stroke="#fbcfe8" />
        <text x="677" y="270" text-anchor="middle" font-size="9.5" font-weight="700" fill="#9d174d">Cruzar la frontera</text>
        <text x="677" y="288" text-anchor="middle" font-size="8.5" fill="#334155">NotificationDispatchRequested → Notifications</text>
        <text x="677" y="303" text-anchor="middle" font-size="8" fill="#64748b">contrato protobuf versionado</text>

        <path d="M850 76 Q 872 20 105 22" fill="none" stroke="#94a3b8" stroke-width="1.2" stroke-dasharray="4,3" marker-end="url(#pcArrow)" />
        <text x="470" y="16" text-anchor="middle" font-size="7.5" fill="#64748b">cuando el comando programado llega, el ciclo vuelve a empezar</text>
    </svg>
    <p class="ddd-diagram-caption">
        La línea punteada de arriba es lo que convierte esto en un sistema vivo: un comando programado
        produce eventos nuevos, que disparan procesos nuevos, que programan más comandos.
    </p>
</div>

<Callout kind="advertencia" title="Process manager, no saga (y no es lo mismo que una proyección)">

Lo que hacen `ListingLifecycleProcess` y `VisitProcess` se llama **process manager**: escuchan eventos y **emiten comandos**. No hay compensaciones ni transacciones distribuidas, así que no es una *saga* en el sentido estricto.

Y la diferencia con una proyección es la que importa:

| | Proyección | Process manager |
| --- | --- | --- |
| Qué hace con el evento | Actualiza una vista | **Provoca cambios** |
| Efectos secundarios | Ninguno | Comandos, mensajes, agregados nuevos |
| Se puede reconstruir | **Sí**, borrando y reproyectando | **No** — reejecutarlo mandaría correos otra vez |

Esa última fila es crítica: si un día reconstruyes las proyecciones, **no debes volver a pasar la historia por los procesos**. Es un error que se comete una vez y se recuerda para siempre.

</Callout>

<Callout kind="resumen" title="Para recordar">

- Los procesos leen los eventos **ya guardados**, después del commit, en su propio scope y su propia transacción.
- Un proceso puede ejecutarse dos veces: sobrevive porque los métodos de agregado son **idempotentes**.
- **Programar el futuro** es publicar un comando con hora de entrega; el agregado decide al llegar si sigue teniendo sentido.
- Modificar otros agregados desde un proceso es la forma correcta de cumplir "una transacción, un agregado".
- La traducción de evento de dominio a **evento de integración** es explícita y es donde decides qué haces público.
- Proyección ≠ process manager: una se puede reconstruir, el otro **no**.

</Callout>

<Callout kind="ejercicio" title="Comprueba que lo entendiste">

1. `ProcessRouter` no tiene un `case` para `ListingExpired`. Escríbelo. ¿A qué método de `ListingLifecycleProcess` lo enviarías, y qué efecto tendría de más o de menos respecto a `ListingClosed`?
2. `ExpireListingHandler` no comprueba si la publicación sigue vigente. ¿Dónde está esa comprobación y por qué es mejor ahí?
3. Alguien propone reconstruir todas las proyecciones en producción reprocesando la historia por `DomainEventsSubscription`. ¿Qué pasaría exactamente, y cómo lo harías bien?
4. `VisitProcess.OnConfirmedAsync` programa hasta tres comandos. Si el interesado cancela la visita al día siguiente, ¿quién cancela esos comandos? (Pista: nadie.)

</Callout>
